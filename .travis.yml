sudo: required
language: node_js
node_js:
  - "6.1"
services:
  - docker
before_install:
  - docker pull mcin/cbrain_travis:dev
  - docker run -d -p cb_travis:3001:80 -v "`pwd -P`":/home/cbrain/cbrain_travis --name "cb_travis" --hostname=cb_travis mcin/cbrain_travis:dev
  - docker ps -a 
  - docker logs cb_travis --follow
“scripts”: {
  "test": "make test"
}
